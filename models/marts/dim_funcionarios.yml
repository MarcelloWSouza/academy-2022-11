
version: 2

models:
  - name: dim_funcionarios
    description: Essa é a dimenssão com dados dos funcionários.
    columns:
      - name: sk_funcionario
        description: essa é a chave surrogate key da tabela.
        tests:
          - unique
          - not_null

      - name: id_funcionario

      - name: id_gerente
          